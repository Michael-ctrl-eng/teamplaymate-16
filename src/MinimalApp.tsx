import React from 'react';

const MinimalApp: React.FC = () => {
  return (
    <div style={{ padding: '20px', textAlign: 'center' }}>
      <h1>Minimal App</h1>
      <p>If you can see this, the minimal React app is working!</p>
    </div>
  );
};

export default MinimalApp;
