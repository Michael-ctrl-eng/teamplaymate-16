<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Match Report - {{teamName}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f5f5f5;
        }
        
        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
        }
        
        .header {
            background: linear-gradient(135deg, #4ADE80 0%, #22C55E 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }
        
        .header h1 {
            font-size: 28px;
            margin-bottom: 10px;
            font-weight: 700;
        }
        
        .match-result {
            background-color: rgba(255,255,255,0.1);
            border-radius: 10px;
            padding: 20px;
            margin-top: 20px;
        }
        
        .score-display {
            font-size: 48px;
            font-weight: 900;
            text-align: center;
            margin: 10px 0;
        }
        
        .team-names {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 18px;
            font-weight: 600;
        }
        
        .result-badge {
            display: inline-block;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: 700;
            font-size: 14px;
            margin-top: 10px;
        }
        
        .result-badge.win {
            background-color: #10B981;
            color: white;
        }
        
        .result-badge.loss {
            background-color: #EF4444;
            color: white;
        }
        
        .result-badge.draw {
            background-color: #F59E0B;
            color: white;
        }
        
        .content {
            padding: 30px 20px;
        }
        
        .greeting {
            font-size: 18px;
            margin-bottom: 20px;
            color: #374151;
        }
        
        .match-info {
            background-color: #F9FAFB;
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
        }
        
        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            padding: 5px 0;
            border-bottom: 1px solid #E5E7EB;
        }
        
        .info-row:last-child {
            border-bottom: none;
            margin-bottom: 0;
        }
        
        .info-label {
            font-weight: 600;
            color: #6B7280;
        }
        
        .info-value {
            color: #111827;
            font-weight: 500;
        }
        
        .section {
            margin: 30px 0;
        }
        
        .section-title {
            font-size: 20px;
            font-weight: 700;
            color: #111827;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 2px solid #4ADE80;
        }
        
        .events-list {
            background-color: #F9FAFB;
            border-radius: 10px;
            padding: 20px;
        }
        
        .event-item {
            display: flex;
            align-items: center;
            padding: 10px 0;
            border-bottom: 1px solid #E5E7EB;
        }
        
        .event-item:last-child {
            border-bottom: none;
        }
        
        .event-minute {
            background-color: #4ADE80;
            color: white;
            padding: 4px 8px;
            border-radius: 4px;
            font-weight: 700;
            font-size: 12px;
            min-width: 35px;
            text-align: center;
            margin-right: 15px;
        }
        
        .event-icon {
            font-size: 20px;
            margin-right: 10px;
        }
        
        .event-details {
            flex: 1;
        }
        
        .event-player {
            font-weight: 600;
            color: #111827;
        }
        
        .event-description {
            color: #6B7280;
            font-size: 14px;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin: 20px 0;
        }
        
        .player-card {
            background-color: #F9FAFB;
            border-radius: 10px;
            padding: 15px;
            border-left: 4px solid #4ADE80;
        }
        
        .player-header {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
        }
        
        .player-number {
            background-color: #4ADE80;
            color: white;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            margin-right: 10px;
        }
        
        .player-name {
            font-weight: 600;
            color: #111827;
        }
        
        .player-position {
            color: #6B7280;
            font-size: 12px;
            margin-left: auto;
        }
        
        .player-stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
            margin-top: 10px;
        }
        
        .stat-item {
            text-align: center;
        }
        
        .stat-value {
            font-size: 18px;
            font-weight: 700;
            color: #4ADE80;
        }
        
        .stat-label {
            font-size: 11px;
            color: #6B7280;
            text-transform: uppercase;
        }
        
        .highlights {
            background: linear-gradient(135deg, #FEF3C7 0%, #FDE68A 100%);
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
        }
        
        .highlight-title {
            font-size: 16px;
            font-weight: 700;
            color: #92400E;
            margin-bottom: 15px;
        }
        
        .highlight-item {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
        }
        
        .highlight-icon {
            font-size: 20px;
            margin-right: 10px;
        }
        
        .highlight-text {
            color: #92400E;
            font-weight: 500;
        }
        
        .cta-section {
            background-color: #F3F4F6;
            border-radius: 10px;
            padding: 25px;
            text-align: center;
            margin: 30px 0;
        }
        
        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #4ADE80 0%, #22C55E 100%);
            color: white;
            padding: 12px 30px;
            text-decoration: none;
            border-radius: 25px;
            font-weight: 600;
            margin: 10px;
            transition: transform 0.2s;
        }
        
        .cta-button:hover {
            transform: translateY(-2px);
        }
        
        .footer {
            background-color: #111827;
            color: #9CA3AF;
            padding: 30px 20px;
            text-align: center;
        }
        
        .footer-links {
            margin: 20px 0;
        }
        
        .footer-links a {
            color: #4ADE80;
            text-decoration: none;
            margin: 0 15px;
        }
        
        @media (max-width: 600px) {
            .team-names {
                flex-direction: column;
                gap: 10px;
            }
            
            .score-display {
                font-size: 36px;
            }
            
            .stats-grid {
                grid-template-columns: 1fr;
            }
            
            .player-stats {
                grid-template-columns: repeat(2, 1fr);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>🏆 Match Report</h1>
            <div class="match-result">
                <div class="team-names">
                    <span>{{teamName}}</span>
                    <span>vs</span>
                    <span>{{opponentName}}</span>
                </div>
                <div class="score-display">
                    {{teamScore}} - {{opponentScore}}
                </div>
                <div style="text-align: center;">
                    <span class="result-badge {{resultClass}}">{{result}}</span>
                </div>
            </div>
        </div>

        <!-- Content -->
        <div class="content">
            <div class="greeting">
                Hello {{memberName}}! 👋
                <br>
                Here's your comprehensive match report as a {{memberRole}} of {{teamName}}.
            </div>

            <!-- Match Information -->
            <div class="match-info">
                <div class="info-row">
                    <span class="info-label">📅 Date & Time</span>
                    <span class="info-value">{{matchDate}} at {{matchTime}}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">🏟️ Venue</span>
                    <span class="info-value">{{venue}}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">🏆 Competition</span>
                    <span class="info-value">{{competition}}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">🏠 Playing</span>
                    <span class="info-value">{{#if isHomeTeam}}Home{{else}}Away{{/if}}</span>
                </div>
            </div>

            {{#if events}}
            <!-- Match Events -->
            <div class="section">
                <h2 class="section-title">⚽ Key Match Events</h2>
                <div class="events-list">
                    {{#each events}}
                    <div class="event-item">
                        <div class="event-minute">{{minute}}'</div>
                        <div class="event-icon">{{icon}}</div>
                        <div class="event-details">
                            <div class="event-player">#{{playerNumber}} {{playerName}}</div>
                            {{#if description}}
                            <div class="event-description">{{description}}</div>
                            {{/if}}
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
            {{/if}}

            {{#if playerStats}}
            <!-- Player Statistics -->
            <div class="section">
                <h2 class="section-title">📊 Player Performance</h2>
                <div class="stats-grid">
                    {{#each playerStats}}
                    <div class="player-card">
                        <div class="player-header">
                            <div class="player-number">{{number}}</div>
                            <div>
                                <div class="player-name">{{name}}</div>
                            </div>
                            <div class="player-position">{{position}}</div>
                        </div>
                        <div class="player-stats">
                            <div class="stat-item">
                                <div class="stat-value">{{goals}}</div>
                                <div class="stat-label">Goals</div>
                            </div>
                            <div class="stat-item">
                                <div class="stat-value">{{assists}}</div>
                                <div class="stat-label">Assists</div>
                            </div>
                            <div class="stat-item">
                                <div class="stat-value">{{rating}}</div>
                                <div class="stat-label">Rating</div>
                            </div>
                            <div class="stat-item">
                                <div class="stat-value">{{minutesPlayed}}</div>
                                <div class="stat-label">Minutes</div>
                            </div>
                            <div class="stat-item">
                                <div class="stat-value">{{yellowCards}}</div>
                                <div class="stat-label">Yellow</div>
                            </div>
                            <div class="stat-item">
                                <div class="stat-value">{{redCards}}</div>
                                <div class="stat-label">Red</div>
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
            {{/if}}

            <!-- Match Highlights -->
            {{#if topScorer}}
            <div class="highlights">
                <div class="highlight-title">🌟 Match Highlights</div>
                {{#if topScorer}}
                <div class="highlight-item">
                    <div class="highlight-icon">⚽</div>
                    <div class="highlight-text">
                        <strong>Top Scorer:</strong> #{{topScorer.number}} {{topScorer.name}} ({{topScorer.value}} goals)
                    </div>
                </div>
                {{/if}}
                {{#if topAssister}}
                <div class="highlight-item">
                    <div class="highlight-icon">🎯</div>
                    <div class="highlight-text">
                        <strong>Top Assister:</strong> #{{topAssister.number}} {{topAssister.name}} ({{topAssister.value}} assists)
                    </div>
                </div>
                {{/if}}
                {{#if topRated}}
                <div class="highlight-item">
                    <div class="highlight-icon">⭐</div>
                    <div class="highlight-text">
                        <strong>Highest Rated:</strong> #{{topRated.number}} {{topRated.name}} ({{topRated.value}} rating)
                    </div>
                </div>
                {{/if}}
            </div>
            {{/if}}

            <!-- Call to Action -->
            <div class="cta-section">
                <h3 style="margin-bottom: 15px; color: #374151;">📱 Continue Your Journey</h3>
                <p style="margin-bottom: 20px; color: #6B7280;">Access detailed analytics, training plans, and team insights on the Statsor platform.</p>
                <a href="{{appUrl}}/matches" class="cta-button">View Full Match Analysis</a>
                <a href="{{appUrl}}/dashboard" class="cta-button">Go to Dashboard</a>
            </div>

            <!-- Feedback Section -->
            <div style="background-color: #F0FDF4; border-radius: 10px; padding: 20px; margin: 20px 0; text-align: center;">
                <h4 style="color: #166534; margin-bottom: 10px;">💬 How was this match report?</h4>
                <p style="color: #166534; margin-bottom: 15px;">Help us improve by sharing your feedback!</p>
                <a href="{{appUrl}}/feedback" style="color: #16A34A; text-decoration: none; font-weight: 600;">Share Feedback →</a>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <div style="font-size: 18px; font-weight: 600; color: #4ADE80; margin-bottom: 10px;">
                ⚽ Statsor - Your Football Management Platform
            </div>
            <p style="margin-bottom: 15px;">Empowering teams with data-driven insights and comprehensive match analysis.</p>
            
            <div class="footer-links">
                <a href="{{appUrl}}">Dashboard</a>
                <a href="{{appUrl}}/matches">Matches</a>
                <a href="{{appUrl}}/players">Players</a>
                <a href="{{appUrl}}/analytics">Analytics</a>
                <a href="{{appUrl}}/support">Support</a>
            </div>
            
            <div style="font-size: 12px; color: #6B7280; margin-top: 20px;">
                <p>© {{year}} Statsor. All rights reserved.</p>
                <p>Questions? Contact us at <a href="mailto:{{supportEmail}}" style="color: #4ADE80;">{{supportEmail}}</a></p>
                <p style="margin-top: 10px;">
                    <a href="{{appUrl}}/unsubscribe" style="color: #6B7280; text-decoration: none;">Unsubscribe</a> | 
                    <a href="{{appUrl}}/privacy" style="color: #6B7280; text-decoration: none;">Privacy Policy</a>
                </p>
            </div>
        </div>
    </div>
</body>
</html>